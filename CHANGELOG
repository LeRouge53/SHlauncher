This is the global changelog, which lists every version (starting from 0.4.1)
======== 0.4.1 ========
 - Added this changelog file (can be viewed with command "about whatsnew")
 - Added return on return command (which terminate the launcher after executing a last command)
 - Added a lot of other argument-based features (like the ability to launch commands directly from bash) and a setting linked to those
 - Added a way to view the log file directly in the launcher ("about log" command)
 - Fixed bugs (kinda)
 - Did not added comments in version.sh
======== 0.4.2 ========
 - Fixed a bug that broke neoforge during installation
 - Added some comments
 - The launcher nows check if it has internet BEFORE trying to use the internet
 - Removed useless and out-of-context lines
======== 0.4.3 ========
 - Added the "config" command (in the form of "instance modify")
 - Added default Minimum/Maximum ram assigned when creating instances (via setting keys)
 - Fixed minor bugs and typos

======== 0.5.0-pre1 ========
 - Added vanilla server support
 - Fixed bugs
 - Still going (I'm gonna work on the github page)
======== 0.5.0-pre2 ========
 - Added a github workflow to use the releases (and the tags) (better late than never)
 - Added neoforge server support (with roughly no bugs)
 - Moved ROR into it's own file (cleaner)
 - Fixed minor bugs
   - Made the library directory path absolute when launching the game
   - "-DbundlerRepoDir" nows no longer points to .minecraft/.minecraft
 - Removed the "assetIndex" category from "version list" results